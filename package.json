{
  "name": "authpaddle",
  "version": "0.0.1",
  "private": true,
  "description": "TypeScript library for auth and payment provider integration",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "happy-dom": "^12.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
